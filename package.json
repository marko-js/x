{
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.54",
    "@babel/core": "^7.0.0-beta.54",
    "@babel/node": "^7.0.0-beta.54",
    "@babel/plugin-proposal-object-rest-spread": "^7.0.0-beta.54",
    "@babel/plugin-transform-runtime": "^7.0.0-beta.54",
    "@babel/preset-env": "^7.0.0-beta.54",
    "@babel/register": "^7.0.0-beta.54",
    "@commitlint/cli": "^7.0.0",
    "@commitlint/config-conventional": "^7.0.1",
    "@commitlint/config-lerna-scopes": "^7.0.0",
    "@ebay/browserslist-config": "^1.0.0",
    "babel-plugin-istanbul": "^4.1.6",
    "chai": "^4.1.2",
    "codecov": "^3.0.4",
    "cross-env": "^5.2.0",
    "eslint": "^5.2.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-prettier": "^2.6.2",
    "husky": "^0.14.3",
    "lerna": "2.11.0",
    "lint-staged": "^7.2.0",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "prettier": "^1.13.7"
  },
  "lint-staged": {
    "*.{json,md}": [
      "prettier --write",
      "git add"
    ],
    "*.js": [
      "prettier --write",
      "git add"
    ]
  },
  "scripts": {
    "build": "lerna exec --parallel -- babel ./src --out-dir ./dist --delete-dir-on-start --config-file ../../babel.config.js",
    "build:watch": "lerna exec --parallel -- babel ./src --watch --out-dir ./dist --delete-dir-on-start",
    "clean": "lerna clean && rm -rf ./packages/*/dist",
    "commitmsg": "echo $GIT_PARAMS && commitlint -e $GIT_PARAMS",
    "format": "prettier \"**/*.{json,md,js}\" --write",
    "lint": "eslint -f visualstudio packages/",
    "postinstall": "lerna bootstrap --hoist",
    "precommit": "lint-staged",
    "publish": "npm run build && lerna publish --independent",
    "mocha": "mocha -r @babel/register packages/*/test/*.test.js",
    "test": "cross-env NODE_ENV=test nyc npm run mocha",
    "report": "open ./coverage/lcov-report/index.html",
    "ci:test": "cross-env NODE_ENV=test nyc --reporter=text npm run mocha",
    "ci:codecov": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  }
}
