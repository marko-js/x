import a from "b";
export { something };

static {
  doThings();
  andStuff();
  function more() {
    abc();
  }
}

class {
  onCreate() {
    this.stuff();
  }
}

<div a={a: 1}>
  <!--123-->
  <html-comment>abc</html-comment>
  <div c=1/>
  <div d=1/>
  <if=(x === a)>a</>
  <else if=(x === 2)>
    b
  </else>
  <else>c</else>
</div>
<div b=1/>

<if=cond>
</if>

<for(i) from=0 to=10 step=2>
  <div c=1/>
</for>

<for(key, val) in=obj>
  <div c=1/>
</for>

<for(val, i) of=arr>
  <div c=1/>
</for>

<div marko-preserve-whitespace>

  Hi

</div>

<div body-only-if=true>
  Hi
</div>