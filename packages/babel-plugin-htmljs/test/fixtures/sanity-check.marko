import a from "b";
export { something };

static {
  doThings();
  andStuff();
  function more() {
    abc();
  }
}

class {
  onCreate() {
    this.stuff();
  }
}

<other x=1 ...thing b={ a: 1 } ...c/>

<div#a.b class="c" a={a: 1} c  =  "${d}">
  ${a}
  <!--123-->
  <html-comment>abc</html-comment>
  <div c=1/>
  <div d=1/>
  <if=(x === a)>a ${b}</>
  <else if=(x === 2)>
    b
  </else>
  <else>c</else>
</div>
<div b=1/>

<span ...abc/>

<if=cond>
  Hello ${planet}
</if>

<for(i) from=0 to=10 step=2>
  <div c=1/>
</for>

<for(key, val) in=obj>
  <div c=1/>
</for>

<for(val, i) of=arr by="name">
  $ i
  <div c=1/>
</for>

<div marko-preserve-whitespace>

  Hi

</div>

<div body-only-if=true>
  Hi
</div>